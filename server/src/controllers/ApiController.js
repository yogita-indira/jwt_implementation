import Course from "../schemas/course_model";
export default class ApiController{
    static async CourseUpload(req, res){
        try{


const {title, description, duration, instructor, category, createdAt } =req.body;


        }catch{

        }
    }
}